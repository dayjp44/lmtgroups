<%# if current_user.admin? %>
	<div class="adminPadding adminFont">
		<%= error_messages_for :role %>
		<% form_for :role, @role, :url => admin_role_path, :html => { :method => :put } do |f| -%>
			<%= render :partial => f %>
			<p><%= submit_tag 'Update' %></p>
		<% end -%>
	</div>
<%# end %>